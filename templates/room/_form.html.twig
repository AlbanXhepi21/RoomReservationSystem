{{ form_start(form) }}
<div class="d-flex flex-row align-items-center mb-4">
    <i class="fas fa-user fa-lg me-3 fa-fw"></i>
    <div class="form-outline flex-fill mb-0">
        {{ form_label(form.name, 'Name', {'label_attr': {'class': 'form-label'}}) }}
        {{ form_widget(form.name, {'attr': {'class': 'form-control'}}) }}
    </div>
</div>

<div class="d-flex flex-row align-items-center mb-4">
    <i class="fas fa-user fa-lg me-3 fa-fw"></i>
    <div class="form-outline flex-fill mb-0">
        {{ form_label(form.capacity, 'Capacity', {'label_attr': {'class': 'form-label'}}) }}
        {{ form_widget(form.capacity, {'attr': {'class': 'form-control'}}) }}
    </div>
</div>


<div class="d-flex flex-row align-items-center mb-4">
    <i class="fas fa-user fa-lg me-3 fa-fw"></i>
    <div class="form-outline flex-fill mb-0">
        {{ form_label(form.building, 'Building', {'label_attr': {'class': 'form-label'}}) }}
        {{ form_widget(form.building, {'attr': {'class': 'form-control'}}) }}
    </div>
</div>

<button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
